const Sequelize = require('sequelize');
const instanciadb = require('../../db');

const columns = {
    nome: {
        type: Sequelize.STRING,
        allow
    }


    email: {
        type: Sequelize
    }
}